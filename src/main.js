import { createApp } from 'vue'
import App from './App.vue'
import 'bootstrap/dist/css/bootstrap.min.css'   // 引入 Bootstrap 样式
import 'bootstrap/dist/js/bootstrap.bundle.min.js' // 可选，引入交互组件

createApp(App).mount('#app')
